// kind:"workflow" marketplace extension. The template DAG is authored as a
// Cinatra BPMN Profile 1.0 sidecar at `cinatra/workflow.bpmn` — parsed +
// compiled to a WorkflowSpec at install time by parseWorkflowBpmnSidecar.
// The companion `cinatra/dashboard.json` declares the operator blog workspace
// portlet composition. No runtime code surface.
export {};
